[build-system]
requires = ["setuptools>=64", "pybind11>=2.10"]
build-backend = "setuptools.build_meta"

[project]
name = "ml_split"
version = "0.1.0"
description = "Outgroup-based subtree merge for divide-and-conquer tree building"
requires-python = ">=3.8"
